<% provide(:title, "Edit game log") %>
<h1>mise à jour de log</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@game) do |f| %>
      <%= render 'shared/error_messages' %>

      <%= f.label :gamelog %>
      <%= f.text_area :gamelog %>


      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>

  </div>
</div>
